"use strict";
//Write a function called make_shirt() that accepts a size
// and the text of a message that should be printed on the shirt.
function make_shirt() {
    let size = "large";
    let message = " the shirt is in black and size is ";
    console.log(` ${message}  "${size}" message: alone warrior.`);
}
make_shirt();
